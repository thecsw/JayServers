#!/bin/sh

apt-get update && apt-get install -y openssh-server

mkdir /var/run/sshd
chmod 0755 /var/run/sshd
/usr/sbin/sshd

useradd --create-home --shell /bin/bash box1user1
passwd box1user1 < /root/box1user1.pass

echo 'Done.'
